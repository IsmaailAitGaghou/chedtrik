# ChedTri9 Frontend

واجهة المستخدم لتطبيق تأجير السيارات الفاخرة ChedTri9، مبني باستخدام React + TypeScript + Vite + Tailwind CSS + shadcn/ui.

## المميزات

- 🎨 تصميم فاخر وحديث مع دعم RTL للغة العربية
- 🚗 عرض السيارات مع فلاتر متقدمة
- 📅 نظام حجز متكامل
- 👤 لوحة تحكم للمستخدمين
- 💳 دعم PayPal للدفع
- 📱 تصميم متجاوب (Mobile-First)

## التقنيات المستخدمة

- **React 18** - مكتبة واجهة المستخدم
- **TypeScript** - للكتابة الآمنة
- **Vite** - أداة البناء السريعة
- **Tailwind CSS** - أداة CSS utility-first
- **shadcn/ui** - مكونات UI قابلة للتخصيص
- **React Router** - التوجيه
- **Axios** - طلبات HTTP
- **Lucide React** - الأيقونات

## التثبيت

```bash
cd frontend
npm install
```

## الإعداد

1. انسخ ملف `.env.example` إلى `.env`:

```bash
cp .env.example .env
```

2. تأكد من أن الـ backend يعمل على `http://localhost:3001`

## التشغيل

```bash
npm run dev
```

التطبيق سيعمل على `http://localhost:5173`

## البناء للإنتاج

```bash
npm run build
```

الملفات المبنية ستكون في مجلد `dist`

## البنية

```
frontend/
├── src/
│   ├── components/      # المكونات القابلة لإعادة الاستخدام
│   │   ├── ui/         # مكونات shadcn/ui
│   │   └── layout/     # مكونات التخطيط
│   ├── pages/          # صفحات التطبيق
│   ├── lib/            # الأدوات والمساعدات
│   ├── types/          # تعريفات TypeScript
│   └── App.tsx         # المكون الرئيسي
├── public/             # الملفات الثابتة
└── package.json
```

## الصفحات

- `/` - الصفحة الرئيسية
- `/cars` - قائمة السيارات
- `/cars/:id` - تفاصيل السيارة
- `/booking/:carId` - صفحة الحجز
- `/login` - تسجيل الدخول
- `/register` - إنشاء حساب
- `/dashboard` - لوحة التحكم

## التخصيص

### الألوان

يمكن تخصيص الألوان في `src/index.css` من خلال متغيرات CSS:

```css
:root {
  --primary: 221.2 83.2% 53.3%;
  /* ... */
}
```

## الدعم

للمزيد من المعلومات، راجع [وثائق shadcn/ui](https://ui.shadcn.com)
